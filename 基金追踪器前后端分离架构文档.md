# 基金追踪器前后端分离架构文档

## 📋 项目概述

本项目是一个基金收益追踪系统，采用前后端分离架构，前端使用Vue.js框架，后端使用Spring Boot框架。用户可以添加基金、记录买入价格和份额，系统自动计算并展示投资收益情况。

## 🏗️ 技术架构

### 前端技术栈
- **框架**: Vue 3 + Vue Router + Pinia
- **UI组件库**: Element Plus
- **HTTP请求**: Axios
- **图表库**: ECharts (用于收益图表展示)
- **构建工具**: Vite
- **开发语言**: TypeScript

### 后端技术栈
- **框架**: Spring Boot 3.x
- **数据库**: MySQL 8.x
- **ORM**: MyBatis Plus
- **安全框架**: Spring Security + JWT
- **缓存**: Redis
- **API文档**: Swagger/OpenAPI
- **构建工具**: Maven

## 📁 项目结构

### 前端项目结构
```
fund-tracker-frontend/
├── src/
│   ├── api/                    # API接口封装
│   │   ├── fund.ts            # 基金相关API
│   │   └── user.ts            # 用户相关API
│   ├── assets/                # 静态资源
│   ├── components/            # 公共组件
│   │   ├── FundCard.vue       # 基金卡片组件
│   │   ├── ProfitChart.vue    # 收益图表组件
│   │   └── AddFundForm.vue    # 添加基金表单组件
│   ├── pages/                 # 页面组件
│   │   ├── Dashboard.vue      # 仪表板页面
│   │   ├── FundList.vue       # 基金列表页面
│   │   ├── FundDetail.vue     # 基金详情页面
│   │   └── Login.vue          # 登录页面
│   ├── stores/                # 状态管理
│   │   ├── fund.ts           # 基金状态管理
│   │   └── user.ts           # 用户状态管理
│   ├── router/                # 路由配置
│   │   └── index.ts
│   ├── utils/                 # 工具函数
│   │   ├── request.ts        # HTTP请求封装
│   │   └── format.ts         # 格式化工具
│   ├── App.vue               # 根组件
│   └── main.ts               # 入口文件
├── public/                   # 公共资源
├── index.html               # HTML模板
├── package.json             # 依赖配置
└── vite.config.ts           # Vite配置
```

### 后端项目结构
```
fund-tracker-backend/
├── src/main/java/com/fundtracker/
│   ├── FundTrackerApplication.java          # 启动类
│   ├── config/                             # 配置类
│   │   ├── SecurityConfig.java             # 安全配置
│   │   ├── RedisConfig.java               # Redis配置
│   │   └── SwaggerConfig.java             # Swagger配置
│   ├── controller/                         # 控制器层
│   │   ├── FundController.java            # 基金控制器
│   │   ├── UserController.java            # 用户控制器
│   │   └── AuthController.java            # 认证控制器
│   ├── service/                           # 业务逻辑层
│   │   ├── FundService.java               # 基金服务
│   │   ├── UserService.java               # 用户服务
│   │   └── FundDataService.java          # 基金数据服务
│   ├── service/impl/                      # 服务实现层
│   ├── repository/                        # 数据访问层
│   │   ├── FundRepository.java           # 基金数据访问
│   │   └── UserRepository.java           # 用户数据访问
│   ├── entity/                           # 实体类
│   │   ├── Fund.java                    # 基金实体
│   │   ├── User.java                    # 用户实体
│   │   └── FundHolding.java             # 基金持仓实体
│   ├── dto/                             # 数据传输对象
│   │   ├── FundDTO.java                # 基金DTO
│   │   └── ProfitDTO.java              # 收益DTO
│   └── util/                            # 工具类
│       └── FundApiUtil.java            # 基金API工具
├── src/main/resources/
│   ├── application.yml                  # 应用配置
│   ├── mapper/                         # MyBatis映射文件
│   └── static/                         # 静态资源
└── pom.xml                            # Maven配置
```

## 🗄️ 数据库设计

### 用户表 (user)
```sql
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `username` VARCHAR(50) UNIQUE NOT NULL,
  `password` VARCHAR(100) NOT NULL,
  `email` VARCHAR(100) UNIQUE,
  `nickname` VARCHAR(50),
  `avatar` VARCHAR(200),
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 基金表 (fund)
```sql
CREATE TABLE `fund` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `fund_code` VARCHAR(20) UNIQUE NOT NULL,
  `fund_name` VARCHAR(100) NOT NULL,
  `fund_type` VARCHAR(20),
  `company` VARCHAR(50),
  `current_price` DECIMAL(10,4),
  `price_date` DATE,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 基金持仓表 (fund_holding)
```sql
CREATE TABLE `fund_holding` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `fund_id` BIGINT NOT NULL,
  `buy_price` DECIMAL(10,4) NOT NULL,
  `shares` DECIMAL(15,2) NOT NULL,
  `buy_date` DATE NOT NULL,
  `total_cost` DECIMAL(15,2) NOT NULL,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`),
  FOREIGN KEY (`fund_id`) REFERENCES `fund`(`id`)
);
```

## 🔌 API接口设计

### 用户认证接口

#### 用户注册
```
POST /api/auth/register
Request Body:
{
  "username": "string",
  "password": "string",
  "email": "string",
  "nickname": "string"
}

Response:
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1,
    "username": "string"
  }
}
```

#### 用户登录
```
POST /api/auth/login
Request Body:
{
  "username": "string",
  "password": "string"
}

Response:
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "jwt-token",
    "user": {
      "id": 1,
      "username": "string",
      "nickname": "string"
    }
  }
}
```

### 基金接口

#### 获取基金列表
```
GET /api/funds?page=1&size=10&keyword=
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [
      {
        "id": 1,
        "fundCode": "000001",
        "fundName": "华夏成长混合",
        "fundType": "混合型",
        "company": "华夏基金",
        "currentPrice": 1.2345,
        "priceDate": "2024-03-20"
      }
    ]
  }
}
```

#### 添加基金持仓
```
POST /api/holdings
Request Body:
{
  "fundCode": "000001",
  "buyPrice": 1.2000,
  "shares": 1000.00,
  "buyDate": "2024-03-15"
}

Response:
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "holdingId": 1
  }
}
```

#### 获取用户持仓
```
GET /api/holdings
Response:
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "fund": {
        "id": 1,
        "fundCode": "000001",
        "fundName": "华夏成长混合"
      },
      "buyPrice": 1.2000,
      "currentPrice": 1.2345,
      "shares": 1000.00,
      "buyDate": "2024-03-15",
      "totalCost": 1200.00,
      "currentValue": 1234.50,
      "profit": 34.50,
      "profitRate": 2.875
    }
  ]
}
```

#### 删除持仓
```
DELETE /api/holdings/{id}
Response:
{
  "code": 200,
  "message": "删除成功"
}
```

### 收益统计接口

#### 获取收益统计
```
GET /api/statistics/profit
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "totalInvestment": 5000.00,
    "currentValue": 5234.50,
    "totalProfit": 234.50,
    "profitRate": 4.69,
    "fundCount": 3
  }
}
```

## 🎨 前端页面设计

### 1. 登录页面 (Login.vue)
- 用户名/密码登录表单
- 注册链接
- 响应式设计

### 2. 仪表板页面 (Dashboard.vue)
- 收益统计卡片（总投资、当前市值、总收益、收益率）
- 收益趋势图表
- 持仓基金概览

### 3. 基金列表页面 (FundList.vue)
- 基金搜索和筛选
- 基金列表展示
- 添加持仓功能

### 4. 我的持仓页面 (MyHoldings.vue)
- 持仓基金列表
- 实时收益显示
- 编辑和删除持仓

### 5. 基金详情页面 (FundDetail.vue)
- 基金详细信息
- 历史价格图表
- 添加持仓入口

## 🔧 核心功能实现

### 前端核心功能

#### 1. API请求封装 (src/utils/request.ts)
```typescript
import axios from 'axios';

const request = axios.create({
  baseURL: '/api',
  timeout: 10000
});

// 请求拦截器
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  }
);

// 响应拦截器
request.interceptors.response.use(
  response => response.data,
  error => {
    // 错误处理
    return Promise.reject(error);
  }
);

export default request;
```

#### 2. 基金状态管理 (src/stores/fund.ts)
```typescript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';

export const useFundStore = defineStore('fund', () => {
  const holdings = ref([]);
  const statistics = ref({
    totalInvestment: 0,
    currentValue: 0,
    totalProfit: 0,
    profitRate: 0
  });

  const totalProfit = computed(() => {
    return statistics.value.currentValue - statistics.value.totalInvestment;
  });

  const profitRate = computed(() => {
    if (statistics.value.totalInvestment === 0) return 0;
    return (totalProfit.value / statistics.value.totalInvestment) * 100;
  });

  return {
    holdings,
    statistics,
    totalProfit,
    profitRate
  };
});
```

### 后端核心功能

#### 1. 基金服务实现 (FundService.java)
```java
@Service
public class FundService {
    
    @Autowired
    private FundRepository fundRepository;
    
    @Autowired
    private FundDataService fundDataService;
    
    public FundDTO getFundByCode(String fundCode) {
        Fund fund = fundRepository.findByFundCode(fundCode);
        if (fund == null) {
            // 从外部API获取基金数据
            FundData fundData = fundDataService.fetchFundData(fundCode);
            fund = convertToFund(fundData);
            fundRepository.save(fund);
        }
        return convertToDTO(fund);
    }
    
    @Scheduled(fixedRate = 300000) // 每5分钟更新一次
    public void updateAllFundPrices() {
        List<Fund> funds = fundRepository.findAll();
        for (Fund fund : funds) {
            try {
                FundData data = fundDataService.fetchFundData(fund.getFundCode());
                fund.setCurrentPrice(data.getCurrentPrice());
                fund.setPriceDate(data.getPriceDate());
                fundRepository.save(fund);
            } catch (Exception e) {
                log.error("更新基金价格失败: " + fund.getFundCode(), e);
            }
        }
    }
}
```

#### 2. 收益计算服务 (ProfitService.java)
```java
@Service
public class ProfitService {
    
    @Autowired
    private FundHoldingRepository holdingRepository;
    
    @Autowired
    private FundRepository fundRepository;
    
    public ProfitStatistics calculateProfit(Long userId) {
        List<FundHolding> holdings = holdingRepository.findByUserId(userId);
        
        BigDecimal totalInvestment = BigDecimal.ZERO;
        BigDecimal currentValue = BigDecimal.ZERO;
        
        for (FundHolding holding : holdings) {
            Fund fund = fundRepository.findById(holding.getFundId());
            
            BigDecimal cost = holding.getBuyPrice().multiply(holding.getShares());
            BigDecimal current = fund.getCurrentPrice().multiply(holding.getShares());
            
            totalInvestment = totalInvestment.add(cost);
            currentValue = currentValue.add(current);
        }
        
        BigDecimal profit = currentValue.subtract(totalInvestment);
        BigDecimal profitRate = totalInvestment.compareTo(BigDecimal.ZERO) > 0 
            ? profit.divide(totalInvestment, 4, RoundingMode.HALF_UP).multiply(new BigDecimal(100))
            : BigDecimal.ZERO;
            
        return ProfitStatistics.builder()
            .totalInvestment(totalInvestment)
            .currentValue(currentValue)
            .totalProfit(profit)
            .profitRate(profitRate)
            .fundCount(holdings.size())
            .build();
    }
}
```

## 🔐 安全设计

### 1. 认证机制
- 使用JWT进行用户认证
- Token存储在HttpOnly Cookie中
- 实现Token刷新机制

### 2. 权限控制
- 基于角色的访问控制
- API级别的权限验证
- 数据级别的权限过滤

### 3. 数据安全
- 密码加密存储（BCrypt）
- SQL注入防护
- XSS攻击防护
- CSRF防护

## 🚀 部署方案

### 开发环境
- 前端：Vite开发服务器 (localhost:3000)
- 后端：Spring Boot内嵌Tomcat (localhost:8080)
- 数据库：本地MySQL
- Redis：本地Redis

### 生产环境
- 前端：Nginx静态文件服务
- 后端：Docker容器部署
- 数据库：云数据库服务
- Redis：云Redis服务
- 负载均衡：Nginx反向代理

## 📈 性能优化

### 前端优化
- 组件懒加载
- 数据缓存
- 图片压缩
- 代码分割

### 后端优化
- 数据库索引优化
- Redis缓存
- 异步处理
- 连接池配置

## 🧪 测试策略

### 单元测试
- 前端：Jest + Vue Test Utils
- 后端：JUnit + Mockito

### 集成测试
- API接口测试
- 数据库集成测试
- 端到端测试

### 性能测试
- 负载测试
- 压力测试
- 并发测试

## 📝 开发流程

1. **环境搭建**
   - 安装Node.js、Java、MySQL、Redis
   - 配置开发环境

2. **数据库初始化**
   - 创建数据库和表结构
   - 配置数据源

3. **后端开发**
   - 搭建Spring Boot项目
   - 实现核心业务逻辑
   - 编写单元测试

4. **前端开发**
   - 搭建Vue项目
   - 实现页面组件
   - 集成API调用

5. **联调测试**
   - 前后端接口联调
   - 功能测试
   - Bug修复

6. **部署上线**
   - 生产环境部署
   - 性能监控
   - 用户反馈收集

## 🔄 维护和升级

### 日常维护
- 基金数据更新
- 系统监控
- 日志分析
- 性能优化

### 功能升级
- 添加新的基金数据源
- 增加投资组合分析
- 添加消息通知功能
- 支持更多投资品种

---

这份文档提供了基金追踪器前后端分离架构的完整蓝图，包括技术选型、项目结构、数据库设计、API接口、核心功能实现等各个方面。开发团队可以根据这份文档进行系统开发和维护。



数据库：jdbc:mysql://localhost:3306

用户：root

密码：@liukai20051126

数据库：fund